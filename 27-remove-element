func removeElement(nums []int, target int) int {
    insertPosition := 0

    for _, v := range nums {
        if v != target {
            nums[insertPosition] = v
            insertPosition++
        }
    } 

    return insertPosition
}

// решение нравится, задача простая, сложность O(n)
